import { StyleSheet } from "react-native";

export default StyleSheet.create({
    accountContainer: {
        bottom: 0,
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        paddingBottom: 50
    },
});
