import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    padding: 12,
    width: '100%',
    display: "flex",
    alignItems: 'center',
    borderRadius: 7,
  },
});
