import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    width: 45,
    height: 45,
    display: "flex",
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
  },
});
